<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Abstract of Quotations</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    /* Base typography */
    :root {
      --font-serif: "Times New Roman", Times, serif;
      --font-sans: "Arial", Helvetica, sans-serif;
      --text-color: #111;
      --muted: #555;
      --border: #333;
      --accent: #000;
    }
    html, body {
      height: 100%;
      background: #fff;
      color: var(--text-color);
    }
    body {
      font-family: var(--font-serif);
      line-height: 1.35;
      margin: 0;
      padding: 0;
    }

    /* Page container for screen preview */
    .page {
      width: 210mm;           /* A4 width */
      min-height: 297mm;      /* A4 height */
      margin: 12mm auto;      /* center on screen */
      padding: 15mm 15mm 18mm 15mm;
      box-sizing: border-box;
      box-shadow: 0 0 0.8mm rgba(0,0,0,0.15);
    }

    /* Header */
    .header {
      text-align: center;
      margin-bottom: 8mm;
    }
    .header .gov-identity {
      font-family: var(--font-sans);
      font-size: 12pt;
      letter-spacing: 0.2px;
    }
    .header .unit {
      font-weight: bold;
      font-size: 14pt;
    }
    .header .location {
      font-size: 12pt;
      color: var(--muted);
    }
    .doc-meta {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2mm 8mm;
      margin-top: 6mm;
      font-size: 12pt;
    }
    .doc-meta .label {
      color: var(--muted);
      font-family: var(--font-sans);
      font-size: 10pt;
    }
    .doc-title {
      margin-top: 6mm;
      text-align: center;
      font-weight: bold;
      text-transform: uppercase;
      font-size: 12pt;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 3mm 0;
    }

    /* Bid table */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 6mm;
      font-size: 11pt;
    }
    colgroup col.label-col { width: 10%; }
    colgroup col.desc-col  { width: 24%; }
    colgroup col.item-col  { width: 10%; }
    colgroup col.offer-col { width: 18%; }
    colgroup col.amount-col{ width: 8%; }

    thead th {
      border: 1px solid var(--border);
      padding: 3mm 2mm;
      text-align: center;
      font-family: var(--font-sans);
      font-size: 10pt;
    }
    tbody td {
      border: 1px solid var(--border);
      padding: 2.5mm 2mm;
      vertical-align: top;
    }
    tbody td.numeric {
      text-align: right;
      font-variant-numeric: tabular-nums;
    }
    tbody td.center {
      text-align: center;
    }

    /* Section headings */
    .section-heading {
      margin-top: 8mm;
      font-weight: bold;
      font-size: 12pt;
      text-transform: uppercase;
    }

    /* Signatories */
    .signatories {
      margin-top: 6mm;
      display: grid;
      gap: 6mm;
    }
    .signatories .group-title {
      font-weight: bold;
      font-size: 11pt;
      text-transform: uppercase;
    }
    .signatories .panel {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 4mm 8mm;
    }
    .signatories .names {
      border: 1px solid var(--border);
      padding: 4mm;
    }
    .signatories .sigline {
      margin-top: 4mm;
      border-top: 1px solid var(--border);
      padding-top: 2mm;
      font-size: 10pt;
      text-align: center;
    }
    .signatories .name {
      font-weight: bold;
      text-align: center;
    }
    .signatories .role {
      text-align: center;
      color: var(--muted);
      font-family: var(--font-sans);
      font-size: 10pt;
    }

    /* Print setup */
    @page {
      size: A4;
      margin: 15mm 15mm 18mm 15mm;
    }
    @media print {
      body {
        background: transparent;
      }
      .page {
        margin: 0;
        box-shadow: none;
        width: auto;
        min-height: auto;
        padding: 0;
      }
      .doc-title {
        break-inside: avoid;
      }
      thead { display: table-header-group; } /* repeat header per page */
      tfoot { display: table-footer-group; }
      /* Avoid orphan headers */
      .section-heading, .signatories { break-inside: avoid; }
    }
  </style>
</head>
<body>
  <div class="page">

    <header class="header">
      <div class="gov-identity">Republic of the Philippines</div>
      <div class="unit">EASTERN VISAYAS STATE UNIVERSITY</div>
      <div class="location">Tacloban City</div>
    </header>

    <section class="doc-meta" aria-label="Document metadata">
      <div>
        <div class="label">Document code</div>
        <div>EVSU-04-SUP-0002</div>
      </div>
      <div>
        <div class="label">PR No.</div>
        <div><!-- {{ pr_number }} -->&nbsp;</div>
      </div>
      <div>
        <div class="label">Bids opened on</div>
        <div><!-- {{ bids_opened_on }} -->&nbsp;</div>
      </div>
      <div>
        <div class="label">Project / Purpose</div>
        <div><!-- {{ project_purpose }} -->&nbsp;</div>
      </div>
    </section>

    <div class="doc-title">ABSTRACT OF PROPOSALS OF BIDS OPENED ON</div>

    <table aria-label="Abstract of quotations">
      <colgroup>
        <col class="label-col" />
        <col class="desc-col" />
        <col class="item-col" />
        <col class="offer-col" />
        <col class="amount-col" />
        <col class="offer-col" />
        <col class="amount-col" />
        <col class="offer-col" />
        <col class="amount-col" />
      </colgroup>
      <thead>
        <tr>
          <th rowspan="2">Qnty</th>
          <th rowspan="2">Unit Articles</th>
          <th rowspan="2">Item No.</th>
          <th colspan="2">GRANDA MANOR</th>
          <th colspan="2">IRONWOOD HOTEL</th>
          <th colspan="2">THE TROPICS AT MACARTHUR PARK</th>
        </tr>
        <tr>
          <th>Offer (if necessary)</th>
          <th>Unit amount</th>
          <th>Offer (if necessary)</th>
          <th>Unit amount</th>
          <th>Offer (if necessary)</th>
          <th>Unit amount</th>
        </tr>
      </thead>
      <tbody>
        <!-- Repeat these rows dynamically for each item -->
        <tr>
          <td class="center"><!-- {{ qty }} -->&nbsp;</td>
          <td><!-- {{ unit_articles }} -->&nbsp;</td>
          <td class="center"><!-- {{ item_no }} -->&nbsp;</td>
          <td><!-- {{ granda.offer }} -->&nbsp;</td>
          <td class="numeric"><!-- {{ granda.unit_amount }} -->&nbsp;</td>
          <td><!-- {{ ironwood.offer }} -->&nbsp;</td>
          <td class="numeric"><!-- {{ ironwood.unit_amount }} -->&nbsp;</td>
          <td><!-- {{ tropics.offer }} -->&nbsp;</td>
          <td class="numeric"><!-- {{ tropics.unit_amount }} -->&nbsp;</td>
        </tr>
        <!-- ... -->
      </tbody>
      <!-- Optional: tfoot for totals if needed in future -->
      <!--
      <tfoot>
        <tr>
          <td colspan="3" class="numeric">Subtotal</td>
          <td colspan="2" class="numeric"><!-- {{ subtotal_granda }} --></td>
          <td colspan="2" class="numeric"><!-- {{ subtotal_ironwood }} --></td>
          <td colspan="2" class="numeric"><!-- {{ subtotal_tropics }} --></td>
        </tr>
      </tfoot>
      -->
    </table>

    <div class="section-heading">Recommended for purchase on quotations of</div>
    <section class="signatories" aria-label="Recommending members">
      <div class="panel">
        <div class="names">
          <div class="name">DORIS ANN S. ESPINA, CPA, CSEE, Ph.D., JD</div>
          <div class="role">Regular Member</div>
        </div>
        <div class="names">
          <div class="name">VILMAR SILVANO A. SOLIS, CPA</div>
          <div class="role">Regular Member</div>
        </div>
        <div class="names">
          <div class="name">ATTY. RODOLPH B. HERNANDEZ, JR.</div>
          <div class="role">Regular Member</div>
        </div>
        <div class="names">
          <div class="name">DR. MARICHU S. ARMADA</div>
          <div class="role">Provisional BAC Member - Expert</div>
        </div>
      </div>
    </section>

    <div class="section-heading">Approved</div>
    <section class="signatories" aria-label="Approving officers">
      <div class="panel">
        <div class="names">
          <div class="sigline"></div>
          <div class="name">DANILO B. PULMA, DM</div>
          <div class="role">Provisional BAC Member - End-user</div>
        </div>
        <div class="names">
          <div class="sigline"></div>
          <div class="name">ANALYN C. ESPAÑO, DA</div>
          <div class="role">BAC Vice-Chairperson</div>
        </div>
        <div class="names">
          <div class="sigline"></div>
          <div class="name">LYDIA M. MORANTE, DA</div>
          <div class="role">BAC Chairperson</div>
        </div>
        <div class="names">
          <div class="sigline"></div>
          <div class="name">DENNIS C. DE PAZ, Ph.D.</div>
          <div class="role">University President</div>
        </div>
      </div>
    </section>

  </div>
</body>
</html>